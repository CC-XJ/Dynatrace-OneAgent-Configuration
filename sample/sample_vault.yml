# The dir should be in /group_vars or <host-vars>/<group-name>/vault.yml
# To encrypt vault file, use command ansible-vault encrypt <file-name>

vault_dt_api_token: <api-token>
vault_dt_env_id: <env-id>
vault_ansible_password: <password>